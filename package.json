{
  "name": "app-services-sdk-js-monorepo",
  "private": true,
  "devDependencies": {},
  "scripts": {
    "generate": "./scripts/generate.sh",
    "wrun": "yarn workspaces run",
    "build":"yarn workspaces run build",
    "release":"yarn workspaces run publish"
  },
  "workspaces": [
    "packages/*"
  ]
}
